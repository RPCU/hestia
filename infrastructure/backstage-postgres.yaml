apiVersion: kustomize.toolkit.fluxcd.io/v1
kind: Kustomization
metadata:
  name: backstage-postgres
  namespace: flux-system
spec:
  interval: 10m
  sourceRef:
    kind: GitRepository
    name: hestia
    namespace: flux-system
  path: ./infrastructure/backstage-postgres
  prune: true
  timeout: 5m
