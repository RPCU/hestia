apiVersion: kustomize.toolkit.fluxcd.io/v1
kind: Kustomization
metadata:
  name: cnpg
  namespace: flux-system
spec:
  interval: 1m
  sourceRef:
    kind: GitRepository
    name: atlas
    namespace: flux-system
  path: ./infrastructure/cnpg
  prune: true
  wait: true
  timeout: 5m
